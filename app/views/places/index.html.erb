<%= link_to 'Add new', "/places/new" %>
<ol>
  <% @places.each do |place| %>
    <li>
      <p>
        <%= link_to place.title, "/places/#{place.id}" %>
        ($ <%= place.admission_price %>) <%= link_to 'Delete!', "/places/#{place.id}/delete" %>
      </p>
    </li>
  <% end %>
</ol>
